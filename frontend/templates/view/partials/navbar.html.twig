<nav class="bg-white shadow-md py-4 px-6 flex justify-between items-center">
    <h1 class="text-xl font-bold">ShopNest</h1>
    <div class="space-x-4">
        <a href="{{ path('home') }}" class="text-gray-700 hover:text-blue-600">Home</a>
        <a href="{{ path('product_list') }}" class="text-gray-700 hover:text-blue-600">Products</a>
        <a href="{{ path('client_list') }}" class="text-gray-700 hover:text-blue-600">Clients</a>
        <a href="{{ path('facture_list') }}" class="text-gray-700 hover:text-blue-600">Invoices</a>
    </div>
</nav>